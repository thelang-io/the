main {
  a := 1
  mut b := a
  b = a
  mut c := a + b
  c = a + b
  mut d := a + b + c
  d += a + b + c
  e := true ? a : b + c
  f := true ? c + d + e : (b = a + c)
}
===== code =====
#include <stdbool.h>
#include <stdint.h>
int main (void) {
  int32_t c;
  int32_t d;
  int32_t e;
  int32_t f;
  int32_t a = 1;
  int32_t b = a;
  b = a;
  c = a + b;
  c = a + b;
  d = a + b + c;
  d += a + b + c;
  e = true ? a : b + c;
  f = true ? c + d + e : (b = a + c);
}
===== output =====
