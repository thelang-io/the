main {

  a := "";
  b := "Hello, World!";
  c := "Multiline\nString";
  d := "Multiline
String
Multiline";

}
===== code =====
#include <the/macro.h>
#include <the/string.h>
int main (void) {
  the_str_t a = the_str_alloc(L"");
  the_str_t b = the_str_alloc(L"Hello, World!");
  the_str_t c = the_str_alloc(L"Multiline\nString");
  the_str_t d = the_str_alloc(L"Multiline" THE_EOL "String" THE_EOL "Multiline");
  the_str_free(d);
  the_str_free(c);
  the_str_free(b);
  the_str_free(a);
}
===== output =====
