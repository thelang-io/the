main {
  var1: int[]
  var2: str[]
}
===== code =====
#include <the/array.h>
#include <the/number.h>
#include <the/string.h>
#include <stdint.h>
THE_ARRAY_DECLARE(int, int32_t)
THE_ARRAY_DEFINE(int, int32_t, int32_t, element, lhs_element == rhs_element, (void) element, the_int_str(element))
int main (void) {
  the_arr_int_t var1_0 = the_arr_int_alloc(0);
  the_arr_str_t var2_0 = the_arr_str_alloc(0);
  the_arr_str_free(var2_0);
  the_arr_int_free(var1_0);
}
===== output =====
