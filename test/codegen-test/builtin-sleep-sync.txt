======= stdin =======
main {
  sleepSync(0)
  sleepSync(10)
  sleepSync(999)
}
======= code =======
#if defined(WIN32) || defined(_WIN32) || defined(__WIN32) || defined(__WIN32__)
  #define THE_OS_WINDOWS
  #define THE_EOL "\r\n"
#else
  #if defined(__APPLE__)
    #define THE_OS_MACOS
  #elif defined(__linux__)
    #define THE_OS_LINUX
  #endif
  #define THE_EOL "\n"
#endif

#include <stdint.h>
#ifndef THE_OS_WINDOWS
  #include <unistd.h>
#endif
#ifdef THE_OS_WINDOWS
  #include <windows.h>
#endif

void sleep_sync (uint32_t);

void sleep_sync (uint32_t i) {
  #ifdef THE_OS_WINDOWS
    Sleep((unsigned int) i);
  #else
    usleep((unsigned int) (i * 1000));
  #endif
}

int main () {
  sleep_sync(0);
  sleep_sync(10);
  sleep_sync(999);
}
======= flags =======
======= stdout =======
