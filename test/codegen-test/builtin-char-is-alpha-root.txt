======= stdin =======
main {
  '\0'.isAlpha()
  '\n'.isAlpha()
  '\t'.isAlpha()
  'A'.isAlpha()
  'a'.isAlpha()
  '0'.isAlpha()
  '9'.isAlpha()
  '_'.isAlpha()
  '-'.isAlpha()
}
======= code =======
#include <ctype.h>
#include <stdbool.h>

bool char_is_alpha (char);

bool char_is_alpha (char c) {
  return isalpha(c);
}

int main () {
  char_is_alpha('\0');
  char_is_alpha('\n');
  char_is_alpha('\t');
  char_is_alpha('A');
  char_is_alpha('a');
  char_is_alpha('0');
  char_is_alpha('9');
  char_is_alpha('_');
  char_is_alpha('-');
}
======= flags =======
======= stdout =======
