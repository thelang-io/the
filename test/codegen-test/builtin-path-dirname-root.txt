======= stdin =======
main {
  path_dirname("")
  path_dirname("t")
  path_dirname("test")
  path_dirname("/")
  path_dirname("t/")
  path_dirname("test/")
  path_dirname("/t")
  path_dirname("/test")
  path_dirname("t/t")
  path_dirname("t/test")
  path_dirname("test/t")
  path_dirname("test/test")
  path_dirname("/t/t")
  path_dirname("/t/test")
  path_dirname("/test/t")
  path_dirname("/test/test")
  path_dirname("/t/t/")
  path_dirname("/t/test/")
  path_dirname("/test/t/")
  path_dirname("/test/test/")
  path_dirname("/t/t/t")
  path_dirname("/t/test/test")
  path_dirname("/test/t/t")
  path_dirname("/test/test/test")
}
======= code =======
#if defined(WIN32) || defined(_WIN32) || defined(__WIN32) || defined(__WIN32__)
  #define THE_OS_WINDOWS
  #define THE_EOL "\r\n"
#else
  #if defined(__APPLE__)
    #define THE_OS_MACOS
  #elif defined(__linux__)
    #define THE_OS_LINUX
  #endif
  #define THE_EOL "\n"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct str {
  char *d;
  size_t l;
};

void *alloc (size_t);
void *re_alloc (void *, size_t);
struct str path_dirname (struct str);
struct str str_alloc (const char *);
void str_free (struct str);

void *alloc (size_t l) {
  void *r = malloc(l);
  if (r == NULL) {
    fprintf(stderr, "Error: failed to allocate %zu bytes" THE_EOL, l);
    exit(EXIT_FAILURE);
  }
  return r;
}
void *re_alloc (void *d, size_t l) {
  void *r = realloc(d, l);
  if (r == NULL) {
    fprintf(stderr, "Error: failed to reallocate %zu bytes" THE_EOL, l);
    exit(EXIT_FAILURE);
  }
  return r;
}
struct str path_dirname (struct str s) {
  if (s.l == 0) {
    str_free(s);
    return str_alloc(".");
  }
  #ifdef THE_OS_WINDOWS
    char e = '\\';
  #else
    char e = '/';
  #endif
  size_t l = 0;
  unsigned char k = 0;
  for (size_t i = s.l - 1;; i--) {
    if (k == 1 && s.d[i] == e) {
      l = i;
      break;
    } else if (k == 0 && s.d[i] != e) {
      k = 1;
    }
    if (i == 0) break;
  }
  if (l == 0) {
    s.d = re_alloc(s.d, 1);
    if (s.d[0] != e) s.d[0] = '.';
    return (struct str) {s.d, 1};
  }
  char *d = alloc(l);
  memcpy(d, s.d, l);
  str_free(s);
  return (struct str) {d, l};
}
struct str str_alloc (const char *r) {
  size_t l = strlen(r);
  char *d = alloc(l);
  memcpy(d, r, l);
  return (struct str) {d, l};
}
void str_free (struct str s) {
  free(s.d);
}

int main () {
  str_free((struct str) path_dirname(str_alloc("")));
  str_free((struct str) path_dirname(str_alloc("t")));
  str_free((struct str) path_dirname(str_alloc("test")));
  str_free((struct str) path_dirname(str_alloc("/")));
  str_free((struct str) path_dirname(str_alloc("t/")));
  str_free((struct str) path_dirname(str_alloc("test/")));
  str_free((struct str) path_dirname(str_alloc("/t")));
  str_free((struct str) path_dirname(str_alloc("/test")));
  str_free((struct str) path_dirname(str_alloc("t/t")));
  str_free((struct str) path_dirname(str_alloc("t/test")));
  str_free((struct str) path_dirname(str_alloc("test/t")));
  str_free((struct str) path_dirname(str_alloc("test/test")));
  str_free((struct str) path_dirname(str_alloc("/t/t")));
  str_free((struct str) path_dirname(str_alloc("/t/test")));
  str_free((struct str) path_dirname(str_alloc("/test/t")));
  str_free((struct str) path_dirname(str_alloc("/test/test")));
  str_free((struct str) path_dirname(str_alloc("/t/t/")));
  str_free((struct str) path_dirname(str_alloc("/t/test/")));
  str_free((struct str) path_dirname(str_alloc("/test/t/")));
  str_free((struct str) path_dirname(str_alloc("/test/test/")));
  str_free((struct str) path_dirname(str_alloc("/t/t/t")));
  str_free((struct str) path_dirname(str_alloc("/t/test/test")));
  str_free((struct str) path_dirname(str_alloc("/test/t/t")));
  str_free((struct str) path_dirname(str_alloc("/test/test/test")));
}
======= flags =======
======= stdout =======
